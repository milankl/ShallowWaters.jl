# This script sets up a couple of computational constants
# according to the used number type Numtype

const one_over_dx = Numtype(1/dx)
const one_over_dx² = Numtype(1/dx^2)
const zeero = Numtype(0.)
const oone = Numtype(1.)
const twoo = Numtype(2.)
const one_minus_α_half = Numtype(1-0.5*α)
const one_over_dx_α = Numtype(α/dx)
const one_half = Numtype(0.5)
const one_quarter = Numtype(0.25)
const four = Numtype(4.)
const four_plus_α = Numtype(4. + α)
const four_minus_α = Numtype(4. - α)

const gg = Numtype(g)
const HH = Numtype(H)
